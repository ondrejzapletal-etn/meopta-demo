{
  "fields": [
    {
      "name": "title",
      "type": "text",
      "required": true
    },
    {
      "name": "description",
      "type": "richText"
    }
  ],
  "notes": [
    "Found 3 inline SVGs — may need icon select field",
    "Form elements detected — may need special handling"
  ]
}